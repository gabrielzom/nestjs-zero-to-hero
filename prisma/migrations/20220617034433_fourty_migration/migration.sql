BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[tab_tasks] ALTER COLUMN [status] NVARCHAR(11) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
